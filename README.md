A collection of model- and harness- agnostic agent configurations.

## Usage

For example to use the `editor` agent:

```
Using the instructions in $AGENTS_PATH/editor/profile.md, review draft.md
```
